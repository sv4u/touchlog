# touchlog Configuration File
# 
# This file should be placed at: ~/.config/touchlog/config.yaml
# (or $XDG_CONFIG_HOME/touchlog/config.yaml)
#
# Template files should be placed at: ~/.local/share/touchlog/templates/
# (or $XDG_DATA_HOME/touchlog/templates/)

# List of available templates
# Each template has a display name and a corresponding template file
templates:
  - name: "Daily Note"
    file: "daily.md"
  - name: "Meeting Notes"
    file: "meeting.md"
  - name: "Book Note"
    file: "book-note.md"
  - name: "Zettelkasten Note"
    file: "zettelkasten.md"
  - name: "Project Note"
    file: "project-note.md"
  - name: "Journal Entry"
    file: "journal.md"

# Directory where notes will be saved
# Use an absolute path or a path relative to your home directory
notes_directory: "~/Documents/notes"

# Date/Time/Datetime variable configuration
# Configure how date, time, and datetime variables are formatted in templates
# If not specified, all variables are enabled with default formats
datetime_vars:
  date:
    enabled: true
    format: "2006-01-02"  # Go time format (YYYY-MM-DD)
  time:
    enabled: true
    format: "15:04:05"    # Go time format (HH:MM:SS)
  datetime:
    enabled: true
    format: "2006-01-02 15:04:05"  # Go time format (YYYY-MM-DD HH:MM:SS)

# Custom variables
# Define your own static variables that can be used in templates
# Variable names cannot conflict with reserved names: date, time, datetime
variables:
  author: "Your Name"
  project: "My Project"
  location: "Home Office"

# Vim mode
# Enable vim keymap support for the full application
# When enabled, you can use vim keybindings for navigation and editing
vim_mode: false

