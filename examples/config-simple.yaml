# touchlog Simple Configuration Example
#
# This is a minimal configuration file with just the essentials.
# Copy this to ~/.config/touchlog/config.yaml (or $XDG_CONFIG_HOME/touchlog/config.yaml)
# and customize as needed.

# Directory where notes will be saved
notes_directory: "~/Documents/notes"

# List of available templates (file-based)
templates:
  - name: "Daily Note"
    file: "daily.md"
  - name: "Journal"
    file: "journal.md"
